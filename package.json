{
  "name": "airtable-schema-exporter",
  "version": "1.0.0",
  "description": "Exports the schema of an Airtable base.",
  "main": "export-schema.js",
  "scripts": {
    "start": "node export-schema.js"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "dotenv": "^10.0.0"
  }
}
